import Link from "next/link";
import {legalPackage,legalRoutes} from "@/content/legal";
export function LegalCenter({page}:{page:string}){
 const route=legalRoutes[page];
 return <div id="top" className="container legal-center"><header className="page-intro"><p className="eyebrow">LLRD / LEGAL & PRIVACY</p><h1>{route.title}</h1><p className="lead">Responsible technology. Clear information.</p><p>Effective date: {legalPackage.effectiveDate} · Last updated: {legalPackage.updatedDate}</p></header><div className="page-body"><p><Link href="/privacy-requests">Privacy Request Center / Your Privacy Choices</Link></p><nav className="legal-nav" aria-label="Legal policies">{Object.entries(legalRoutes).map(([slug,r])=><Link key={slug} href={'/'+slug} aria-current={slug===page?'page':undefined}>{r.title}</Link>)}</nav><p>LLRD LLC develops technology products and digital services. This center explains the corporate framework. A product-specific policy controls for that product where stated and permitted by law.</p>{page!=="privacy"&&<nav className="legal-toc" aria-label="On this page"><h2>On this page</h2>{route.parts.map(i=><a key={i} href={'#part-'+i}>{legalPackage.parts[i].title}</a>)}</nav>}<div className="legal-copy">{route.parts.map(i=><section id={'part-'+i} key={i}><h2>{legalPackage.parts[i].title}</h2>{legalPackage.parts[i].sections.map(s=><section key={s.title}><h3>{s.title}</h3>{s.paragraphs.map((p,j)=><p key={j}>{p}</p>)}</section>)}<a href="#top">Back to top ↑</a></section>)}<section><h2>Final legal notice</h2>{legalPackage.final.map((p,i)=><p key={i}>{p}</p>)}</section></div></div></div>;
}
