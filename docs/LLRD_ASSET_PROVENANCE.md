# Asset provenance

## Supplied LLRD logo

The user supplied `ChatGPT Image Sep 6, 2026, 08_44_45 PM.png` and explicitly emphasized **LLRD with the star inside R**. The site displays the exact primary logo pixels using an inline SVG viewport onto the untouched supplied board. It does not recreate letterforms or regenerate the star. The logo source is stored in `public/brand/llrd-logo-primary/supplied-brand-reference.png`; the viewport is centralized in `content/brand.ts`. The full board is not presented as the website or treated as confirmed product data.

This is a reference-resolution interim asset. A standalone transparent, high-resolution master is still needed. Setting `brand.logo` replaces the reference viewport without changing the header or footer. Preserve L-L-R-D and the star within R in every export.

## Earth network hero

Original artwork generated with the built-in imagegen tool in response to the user's request for a technically advanced visual appearance. It is separate from the logo, copy and UI, and is not represented as a final master logo. File: `public/brand/earth-sunrise/earth-network.png` (1536 × 1024). Next Image serves optimized responsive versions.

Exact generation prompt:

> Use case: stylized-concept
> Asset type: original cinematic website hero background, wide landscape 1536x1024 or wider.
> Primary request: Earth viewed from low orbit with a brilliant sunrise of warm metallic gold on the right horizon and elegant gold geometric network connections tracing across the globe.
> Reference guidance: inspired by the Earth imagery in a corporate brand board with deep-space near-black #0B1D16, earth-green #1F3D32, sunrise-gold #D4AF37 palette. This is standalone original Earth artwork, not a brand board or logo recreation.
> Scene/backdrop: deep green near-black space, very sparse faint stars.
> Composition/framing: Earth fills the lower half, its atmospheric arc rising from the lower-left toward the middle-right; sun on the right at the horizon. Reserve the upper half and especially upper-left as clean dark negative space for separately rendered headline text. Strong panoramic scale; the right sunrise is a focused warm glow, avoid excessive white flare.
> Style/medium: polished photorealistic cinematic 3D orbital imagery, premium technically advanced corporate feel. Realistic ocean, continental land and softly textured clouds; delicate luminous gold network nodes and triangular connections on the visible surface.
> Constraints: no text, no letters, no logos, no UI, no border, no watermark. Single full-bleed image.
